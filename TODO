1. back to business
2.check what is wrong lets go step by step ok 
3.Hmm since i moved node packages to browser it is failing it should solve if i move from app.tsx to electron.ts localhost
4.For now comment everything and try IPC 
5.What does protocol.handle do??